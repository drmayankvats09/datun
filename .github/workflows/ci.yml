# ═══════════════════════════════════════════════════════════════
# DATUN — CI PIPELINE (Phase 9 — FAANG-grade)
# Runs on every push and PR. Blocks merge if anything fails.
#
# Steps: Install → Generate → Lint → Format → Typecheck →
#        Build → Test → Translation Check → Coverage →
#        CSP Regression Guard (Task #45) → Audit
#
# Pattern: Vercel, Cal.com, Linear — all have 15+ step CI.
# ═══════════════════════════════════════════════════════════════

name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - '.gitignore'
      - '.github/ISSUE_TEMPLATE/**'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - '.gitignore'
      - '.github/ISSUE_TEMPLATE/**'

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '22'
  DATABASE_URL: 'postgresql://postgres:postgres@localhost:5432/datun_shadow_ci'
  TURBO_TELEMETRY_DISABLED: 1

jobs:
  quality:
    name: Lint → Types → Build → Test → i18n
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: datun_shadow_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Cache Turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            turbo-${{ runner.os }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma Client
        run: pnpm turbo run db:generate

      # ─────────────────────────────────────────────────────────────────
      # Task #42 — Migration validation (added Day 16)
      # ─────────────────────────────────────────────────────────────────
      # Verifies that:
      #   1. Schema is valid
      #   2. No drift between schema.prisma and migrations folder
      #   3. Migrations apply cleanly to a fresh shadow DB
      # Postgres service (declared at job level) provides shadow DB.
      - name: Validate Prisma schema
        run: pnpm --filter @repo/db prisma validate

      - name: Check no migration drift
        run: |
          pnpm --filter @repo/db prisma migrate diff \
            --from-schema-datamodel packages/db/prisma/schema.prisma \
            --to-migrations packages/db/prisma/migrations \
            --shadow-database-url "$SHADOW_DATABASE_URL" \
            --exit-code
        env:
          SHADOW_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/datun_shadow_ci

      - name: Sync schema to test DB (Task #43 — covers schema-only models)
        run: pnpm --filter @repo/db exec prisma db push --skip-generate --accept-data-loss
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/datun_shadow_ci

      - name: Apply migrations to shadow DB
        run: pnpm --filter @repo/db prisma migrate deploy
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/datun_shadow_ci

      - name: Lint
        run: pnpm turbo run lint

      - name: Format Check
        run: pnpm format:check

      - name: Typecheck
        run: pnpm turbo run check-types

      # ─────────────────────────────────────────────────────────────────
      # Task #45 — CSP Regression Guard
      # ─────────────────────────────────────────────────────────────────
      # Cheap, deterministic, no network. Fails the PR if anyone introduces
      # 'unsafe-inline' or 'unsafe-eval' into CSP code paths (script context).
      # Runs BEFORE build/test so a failed guard short-circuits the slow
      # downstream steps.
      - name: CSP regression guard — no unsafe-inline / unsafe-eval in script CSP code
        run: |
          set -euo pipefail
          PATHS=(
            "apps/web/lib/csp"
            "apps/web/proxy.ts"
            "apps/web/next.config.ts"
          )
          # Look for 'unsafe-inline' or 'unsafe-eval' but EXCLUDE lines that
          # also reference style-src (style-src 'unsafe-inline' is permitted
          # by design — see ADR-0004). This is conservative: a future addition
          # of 'unsafe-inline' to script-src would be on a different line.
          FOUND=$(grep -rnEH "'unsafe-(inline|eval)'" "${PATHS[@]}" 2>/dev/null \
            | grep -vE "style-src|stylesheets|inline styles|allowed for styles|is ABSENT|recommend 'unsafe-inline'|styleSrcParts" \
            || true)
          if [ -n "$FOUND" ]; then
            echo "::error::CSP regression: 'unsafe-inline' or 'unsafe-eval' found in script-context CSP code"
            echo "$FOUND"
            exit 1
          fi
          echo "✓ No CSP regressions detected in script directives"

      - name: Build
        run: pnpm turbo run build
        env:
          NEXT_PUBLIC_APP_URL: ${{ vars.NEXT_PUBLIC_APP_URL || 'https://datunai.com' }}
          NEXT_PUBLIC_API_URL: ${{ vars.NEXT_PUBLIC_API_URL || 'https://api.datunai.com' }}

      - name: Test with Coverage
        run: pnpm turbo run test:coverage
        env:
          NODE_ENV: 'test'
          JWT_SECRET: 'ci-jwt-secret-minimum-32-characters-long-for-testing'
          JWT_REFRESH_SECRET: 'ci-refresh-secret-minimum-32-characters-different'
          ANTHROPIC_API_KEY: 'sk-ant-ci-test-key'
          DATABASE_URL: 'postgresql://postgres:postgres@localhost:5432/datun_shadow_ci'
          CSP_IP_HASH_SALT: 'ci-csp-ip-hash-salt-fixed-for-deterministic-tests'

      # Phase 9: Translation completeness — blocks merge if Hindi keys missing
      - name: Translation Check (i18n)
        run: cd apps/web && pnpm dlx tsx scripts/check-translations.ts

      # API coverage report on PRs
      - name: API Coverage Report
        if: github.event_name == 'pull_request'
        uses: davelosert/vitest-coverage-report-action@v2
        with:
          name: API Coverage
          vite-config-path: ./apps/api/vitest.config.ts
          json-summary-path: ./apps/api/coverage/coverage-summary.json

      # Web coverage report on PRs
      - name: Web Coverage Report
        if: github.event_name == 'pull_request'
        uses: davelosert/vitest-coverage-report-action@v2
        with:
          name: Web Coverage
          vite-config-path: ./apps/web/vitest.config.ts
          json-summary-path: ./apps/web/coverage/coverage-summary.json

      - name: Security Audit
        run: pnpm audit --audit-level=high --prod
        continue-on-error: false

  security:
    name: Secret Scanning
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  semgrep:
    name: Security Analysis (Semgrep)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    container:
      image: semgrep/semgrep
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - name: Run Semgrep
        run: semgrep scan --config auto --error --severity ERROR

  label:
    name: Auto Label PR
    runs-on: ubuntu-latest
    timeout-minutes: 2
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Label PR
        uses: actions/labeler@v5
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

  purge-cache:
    name: Cloudflare Cache Purge
    runs-on: ubuntu-latest
    timeout-minutes: 2
    needs: [quality]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Purge Cloudflare Cache
        run: |
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" -X POST \
            "https://api.cloudflare.com/client/v4/zones/${{ secrets.CLOUDFLARE_ZONE_ID }}/purge_cache" \
            -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"purge_everything": true}')
          if [ "$RESPONSE" = "200" ]; then
            echo "✅ Cloudflare cache purged successfully"
          else
            echo "⚠️ Cache purge returned HTTP $RESPONSE (non-critical)"
          fi
