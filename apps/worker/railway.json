{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @repo/db db:generate && pnpm --filter @repo/db db:migrate:deploy && pnpm --filter worker build"
  },
  "deploy": {
    "startCommand": "pnpm --filter worker start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "numReplicas": 1
  }
}
