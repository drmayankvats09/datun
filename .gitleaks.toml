# ═══════════════════════════════════════════════════════════════
# GITLEAKS — Secret scanning configuration
# ═══════════════════════════════════════════════════════════════

title = "Datun — Gitleaks Config"

[extend]
useDefault = true

[allowlist]
description = "Global allowlist"
paths = [
  '''pnpm-lock\.yaml''',
  '''\.turbo''',
  '''\.next''',
  '''dist''',
  '''node_modules''',
  '''\.env\.example''',
  '''__tests__''',
  '''vitest\.config''',
  '''\.github/workflows/ci\.yml''',
]